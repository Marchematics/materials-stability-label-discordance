# SourceAware-Stability v2.0.2

This Digital Discovery submission release contains the corrected M1
model-facing evaluation on the 31,872-row all-view common-support cohort.

## Included records

- structure-matched denominator and label-view tables;
- model-specific predicted-hull rankings and elemental reference structures;
- raw-score construct-validity audit;
- fixed-support metrics, top-K tables and exact discovery curves;
- paired chemical-system bootstrap replicates and winner probabilities;
- fixed D2-subsystem phase-pool manifest;
- figure source tables, analysis scripts, tests and pinned environment files.

The frozen version is identified by the GitHub release tag
`v2.0.2-dd-submission`.
