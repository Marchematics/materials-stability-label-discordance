# SourceAware-Stability v2.0.1

Release accompanying **“Source-aware stability labels reshape AI
crystal-discovery benchmarks.”**

Contents include benchmark tables, source provenance, model-evaluation outputs,
figure source data, publication figures, benchmark cards, manifests and tests.

## Reproduction

```bash
conda env create -f environment.yml
conda activate sourceaware-stability
bash run_all.sh
```
